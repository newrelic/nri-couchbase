name: com.newrelic.couchbase
description: Reports status and metrics for Couchbase service
protocol_version: 2
os: windows

commands:
  all_data:
    command:
      - ./bin/nr-couchbase.exe
    interval: 15
    prefix: config/couchbase
