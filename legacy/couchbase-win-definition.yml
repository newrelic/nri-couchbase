name: com.newrelic.couchbase
description: Reports status and metrics for Couchbase service. Legacy file kept for compatibility.
protocol_version: 2
os: windows

commands:
  all_data:
    command:
      - .\bin\nri-couchbase.exe
    interval: 15
    prefix: config/couchbase
